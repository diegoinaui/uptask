doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    style
      include ./email.css
  body 
    h2 Confirma tu cuenta UpTask
    p Ya estás casi inspcrito, presiona el siguiente boton
    a(href=`${confirmarUrl}`) Confirmar Cuenta

    Este enlace es temporal, asi que si se vence vuelve a solicitarlo.
    p Si no puedes acceder ir a este enlace, visita #{confirmarUrl}
